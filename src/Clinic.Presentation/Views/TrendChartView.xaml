<UserControl x:Class="Clinic.Presentation.Views.TrendChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" x:Name="TitleText"
                   FontSize="{StaticResource FontSizeCaption}" FontWeight="SemiBold" Foreground="{StaticResource Gray700Brush}"
                   Margin="0,0,0,6"/>

        <!-- 图表区域 -->
        <Grid Grid.Row="1">
            <Canvas x:Name="ChartCanvas" SizeChanged="ChartCanvas_SizeChanged"/>
        </Grid>
    </Grid>
</UserControl>
