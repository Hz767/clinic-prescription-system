<UserControl x:Class="Clinic.Presentation.Controls.AutoCompleteTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="Transparent">
    <Grid>
        <TextBox x:Name="InputBox"
                 Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}"
                 VerticalContentAlignment="Center"
                 GotFocus="InputBox_GotFocus"
                 LostFocus="InputBox_LostFocus"
                 TextChanged="InputBox_TextChanged"/>

        <Popup x:Name="ResultPopup"
               Placement="Bottom"
               PlacementTarget="{Binding ElementName=InputBox}"
               IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource AncestorType=UserControl}}"
               AllowsTransparency="True"
               StaysOpen="False"
               PopupAnimation="Slide"
               MinWidth="{Binding ActualWidth, ElementName=InputBox}">
            <Border Background="White"
                    BorderBrush="{StaticResource BorderDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Margin="0,2,0,0"
                    Padding="0,4"
                    MaxHeight="280">
                <Grid>
                    <ListBox x:Name="ResultList"
                             ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                             SelectionChanged="ResultList_SelectionChanged"
                             BorderThickness="0"
                             Background="Transparent">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="12,8"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Bd"
                                                    Background="{TemplateBinding Background}"
                                                    Padding="{TemplateBinding Padding}"
                                                    SnapsToDevicePixels="True">
                                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#F0FDFA"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#CCFBF1"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                    <TextBlock x:Name="EmptyHint"
                               Text="无匹配结果"
                               FontSize="{StaticResource FontSizeTiny}"
                               Foreground="{StaticResource TextDisabledBrush}"
                               Padding="12,8"
                               Visibility="Collapsed"/>
                </Grid>
            </Border>
        </Popup>
    </Grid>
</UserControl>
