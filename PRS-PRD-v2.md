
# 个体诊所单机处方管理系统 PRD（v2 修订版）

> 适用对象：惠州市惠城区个人诊所（法人即主诊医生）
> 版本：v2（基于对话评审发现的问题修订）
> 文档目的：作为研发、卫健合规对表、采购、验签的唯一参考

## 0. 目录

1. 项目概述
2. 业务场景与角色
3. 合规约束规范（惠州惠城区强制）
4. 系统整体架构
5. 核心功能详细设计
6. 硬件与部署
7. 非功能需求
8. 测试与验收
9. 风险与开放问题
10. 附录

## 1. 项目概述

### 1.1 项目背景
本项目为惠州市惠城区单人个体诊所定制开发纯本地离线处方诊疗管理系统。系统全程本地电脑运行、无需联网、无云端数据上传、无公网接口。

诊所现状：
- 单医生法人执业
- 接诊量有明显季节性波动（流感季 / 暑期 / 春节前后）
- 现使用的收款方式：① 现金 ② 银联 / 微信 / 支付宝通用商户 POS（非医保专用）
- 是否医保定点：**待第一节确认（见 9.2 §R-1）**

### 1.2 产品定位
合规满足卫健日常抽查 + 药品进销存自动盘点 + 处方病历标准化留存 + 收费对账管理 + 患者基础随访服务。**彻底放弃医保定点适配升级**，聚焦单人诊所的低成本、个性化、高数据安全离线使用需求。

### 1.3 运行模式定义

#### 1.3.1 当前阶段
- 纯本地离线单机版，不上传任何数据至外网
- 不接入广东省医保前置机
- 不作为医保定点 HIS 使用

#### 1.3.2 收费模式（与 9.2 §R-1 决策强绑定）
- **现金**：由本系统记录流水号、金额、对应处方 ID
- **通用商户 POS（银联 / 微信 / 支付宝）**：通过扫码枪或手工录入方式关联至对应处方
- **不支持任何形式的医保个人账户交易**（除非诊所正式取得医保定点资质）

#### 1.3.3 升级边界（明确不再二次开发）
本系统的设计假设是诊所长期保持'单人、非医保定点、纯离线'，业务扩张时不基于本系统迭代，而直接采购商用合规成品 HIS 替换。

### 1.4 开发目标
1. 满足《处方管理办法》《电子病历应用管理规范》《诊所基本标准 2022》及惠城区卫健委抽查要求
2. 单医生极简操作，适配淡旺季接诊波动
3. 处方保存即自动扣减库存、智能盘点、效期预警
4. 现金 + 通用 POS 双通道对账归档
5. 合规轻量患者服务（随访、用药提醒、档案留存），不触营销红线
6. 全链路日志防篡改、可追溯、可导出、可打印

### 1.5 不在范围
- 互联网问诊 / 远程开方 / 在线电子处方流转
- 医保结算 / 医保电子凭证应用
- 多门店 / 多用户协同
- 第三方云端备份、人工客服、推送营销

## 2. 业务场景与角色

### 2.1 主要角色
| 角色 | 是否默认开户 | 权限范围 |
|------|---------|---------|
| 主诊医生（法人） | 是 | 全部业务能力（开方、审方、改作废、盘点、退药、退费、报表、备份） |
| 护士 | 否（保留入口） | 建档、体征录入、发药登记、患者随访、不可开方 |
| 只读审计员 | 否 | 查看病历、处方、库存、日志、报表，不可改 |

> 默认仅开通主诊医生账号。护士 / 只读账号需要时由主诊医生在系统内激活，本地记录激活日志。

### 2.2 典型业务流程
1. 患者到诊 → 护士 / 医生登记或检索档案
2. 主诊医生录入主诉、查体、诊断
3. 主诊医生开方（系统自动校验药品数 + 用药天数 + 配伍禁忌 + 过敏史）
4. 系统计算应收金额，生成处方 PDF（仅电子存档，不含签章图）
5. 患者选择支付方式（现金 / 扫码），关联 POS 流水号或小票图片
6. 系统按 FIFO（近效期优先）自动扣减库存
7. 处方落档、病历落档、日志记录完成
8. 患者离诊；如需随访则建立随访任务

### 2.3 季节性波动场景
- **旺季日接诊 30+（预估峰值）**：单医生不可手动盘点。系统支持'快速抽盘'，只盘点高消耗 / 近效期药品。
- **淡季日接诊 < 5**：支持'全盘导出'，批量录入实盘数，自动比对账实差异。
- **春节 / 国庆停诊**：系统不写虚数据，长期不操作也不会影响药品效期预警的累计规则。

## 3. 合规约束规范（惠州惠城区强制）

### 3.1 上位法规依据
- 《处方管理办法》（卫生部令第53号）
- 《长期处方管理规范（试行）》（国卫办医发〔2021〕17号）
- 《电子病历应用管理规范（试行）》（国卫办医发〔2017〕8号）
- 《医疗机构病历管理规定（2013年版）》（住院病历 ≥ 30年 / 门诊病历 ≥ 15年）
- 《医院处方点评管理规范（试行）》（卫医管发〔2010〕28号）
- 《医疗机构药事管理规定》《抗菌药物临床应用管理办法》
- 《医疗用毒性药品管理办法》（仅作为药品分类参考，本诊所不具备麻精药品处方权）
- 《医疗器械监督管理条例》
- 《个人信息保护法》（医疗数据敏感个人信息）
- 《诊所基本标准（2022年版）》及《诊所备案管理暂行办法》
- 广东省 / 惠州市卫生健康委员会最新公开文件（联系窗口核验）

### 3.2 卫健合规

#### 3.2.1 处方合规
1. **版式**：处方笺符合广东省统一格式（处方前记 / 正文 / 后记），打印输出按既有院内纸质处方笺尺寸生成 PDF。
2. **药品名**：只显示通用名，禁止商品名（系统药品基础数据强制按通用名录入）。
3. **单张处方品种上限**：药品品种总数 ≤ 5（中药饮片与西药**必须分方开具**，此为法规强制要求不可配置；中成药是否与西药同方可根据地方卫健委口径配置）。
4. **用药天数**（依据《处方管理办法》及《长期处方管理规范（试行）》国卫办医发〔2021〕17号）：
   - 急诊 ≤ 3 天
   - 门诊普通 ≤ 7 天
   - 慢病长期处方：一般 ≤ 28 天（4 周）；病情稳定需延长时必须勾选《延长处方》并填写理由，最长不超过 84 天（12 周）；超过 4 周的长期处方须在病历中记录并由患者签字确认
   - 超过 7 天的请求必须弹窗确认且落入延长处方专用统计
5. **无诊断不可开方**。
6. **用法用量**：必须填写且符合药品说明书的合理剂量范围。
7. **修改 / 作废**：原处方 PDF 永久留档，标注 *'作废'*，写明理由与操作时间，操作日志不可清空。
8. **纸质处方签章确认**：纸质处方由医生手写签名盖章后交付患者，处方 PDF 作为电子存档备份，不含签章图；每张处方 PDF 必须包含出方时间（精确到秒）+ 唯一处方编号。

#### 3.2.2 病历合规
1. **必填项策略**：主诉、诊断、处方信息、纸质签名确认 = 硬必填；查体 / 辅助检查 = 软必填（如未做必须勾选 'N/A'，禁止留空）。
2. **修改日志**：任何字段被修改均产生不可删除的溯源记录（字段名、旧值哈希、新值哈希、操作时间、操作者）。
3. **保存期限**（依据《处方管理办法》第50条）：
   - 门诊病历 ≥ 15 年
   - 普通处方、急诊处方 ≥ 1 年
   - 医疗用毒性药品处方 ≥ 2 年（本诊所不经营麻精药品，麻醉/精神药品处方保存条款不适用）
   - 进销存台账 ≥ 3 年

#### 3.2.3 麻精药品管理（不适用）

> **实际情况**：本诊所不具备麻精药品处方权及印鉴卡，不经营麻醉药品、精神药品、医疗用毒性药品。因此：
> - 系统不包含麻精药品专册台账（narcotic_log）功能
> - 系统不包含麻精专方打印模板
> - 系统不包含双人复核子流程
> - 药品基础数据中不设麻精药品分类与标记
> - 处方类型仅保留"普通门诊处方"和"急诊处方"两种，不含"麻精处方"
> - 如未来诊所取得相关资质，需另行采购合规成品 HIS，不在本系统迭代范围内

#### 3.2.4 退药 / 退费 / 作废流程
1. **未取药退费**：保留原处方 PDF，标记 *'未发药退费'*，库存回滚，记录退款流水。
2. **已发药退药**：必须经医生签字 + 患者签字（扫描件留档），按批号回滚库存，台账注明 *'退药'*。
3. **医生主动作废**：写明理由 + 双方签字，操作不可删除。
4. 任何退药 / 退费必须写入 payment_log 与 stock_op_log，不可两账合一。

### 3.3 医保合规（关键决策点：见 9.2 §R-1）

#### 3.3.1 当前默认模式（非医保定点）
- 系统不接入广东省医保前置机
- 不调用任何医保官方接口
- 不出现处方上传、医保电子凭证、CA证书调用
- 通用商户 POS 收款不视为医保交易，仅为对外收银记账

#### 3.3.2 若诊所实际为医保定点（需用户确认）
- 系统扩展点保留：可在临床模块外新增 *'医保接入' *插拔包，由医生按需启用
- 启用后必须包含：医保目录对照、CA电子签名、医保电子处方流转上传、医保结算对账四个子模块
- 此分支在本期 v2 范围内**不实现**，仅在架构层预留接口与目录

### 3.4 隐私与数据安全合规
- 数据库本地加密存储，患者姓名、联系方式、诊断等敏感个人信息禁止明文落库
- 所有数据仅本地留存，无自动外网同步
- 严禁营销推送、批量外发患者信息、未经用户授权的二次利用

### 3.5 数据安全实施细则

#### 3.5.1 物理与网络隔离
- 系统全程离线运行
- 代码层面剔除所有云上传、心跳同步、设备信息上报逻辑
- 本地电脑只接扫码枪 / POS USB HID 设备，不接入任何公网
- 防火墙出站策略由项目开发人/运维人配置（提供 Windows 防火墙配置指引文档）

#### 3.5.2 数据库本地化加密防护
- **v1 加密方案**：标准 SQLite + 敏感字段 AES-GCM 加密（手机号全号加密 + 哈希检索，姓名保留明文，诊断加密存储）
- **P1 升级目标**：SQLCipher 4.x 全库加密（密钥派生使用 PBKDF2-HMAC-SHA256，迭代次数 ≥ 100,000）
- **密钥管理（v1）**：由医生账号密码派生数据库主密钥（PBKDF2），登录时解密敏感字段
- **密钥管理（P1 目标）**：密码 + U 盘密钥双因子派生，任一缺失可重置但需第二次操作确认
- **独占访问**：数据库仅本机系统进程可读写，DB 文件权限收紧到应用运行账户
- **回退方案**：若 SQLCipher 商业授权冲突（P1 升级时），继续使用字段级加密方案（见 9.1 §R-2）

#### 3.5.3 操作防篡改与日志溯源
- **唯一操作权限**：默认仅主诊医生账号拥有核心写入权限；任何账号激活、权限变更写入 system_log，不可删除
- **双日志留存**：业务表日志 + 独立 audit_log.dat 加密文件，按日滚动追加，SHA-256 链式哈希
- **留痕覆盖**：处方操作、病历编辑、库存变动、盘点、报损、收费、退款、POS 绑定、登录登出
- **导出与打印**：每张导出 / 打印操作都落 system_log（操作者、时间、目标文件、哈希）

#### 3.5.4 本地文件隐私安全
- 所有 PDF、小票图片、报表文件存储于诊所本地专用目录，不外发
- 系统不带批量导出 / 一键外发按钮（导出走受控菜单，留痕）
- 严格执行《个人信息保护法》医疗数据合规要求

#### 3.5.5 自主备份与异地冷备
- **自动备份**：每日 18:00 自动备份至用户配置的备份目录（默认为非系统盘的 backup/ 目录，首次启动向导中由用户选择）
- **每周提醒**：提醒医生插入 U 盘冷备（按 Windows 卷影事件检测或由医生手动触发）
- **备份完整性**：备份包自带 SHA-256 校验与 manifest
- **季度演练**：系统内置恢复演练向导，每季度提醒一次
- **保留策略**：本地保留 90 天滚动备份；U 盘保留不少于 12 个月

#### 3.5.6 应急恢复与勒索防护
- 数据库文件落到非系统盘（路径可配置，默认建议 D 盘）
- 配套 Windows Defender 白名单文档（明列 md5 哈希，由项目运维人配置）
- 应急时优先恢复 *昨天的自动备份* 而不是尝试解密受感染文件
- 建议诊所为系统盘配置系统还原点（不在软件交付范围内）

#### 3.5.7 密码与登录安全
- 密码本地二次哈希（v1：PBKDF2-HMAC-SHA256，迭代 ≥ 100,000；P1 升级至 Argon2id）
- 连续失败 5 次锁定 10 分钟（可配置）
- 系统启动时检测关键文件完整性（破坏即报警）

### 3.6 处方签章说明
- **本期不使用电子签名或 CA 证书**。处方 PDF 仅作为诊疗记录的电子存档，纸质处方由医生手写签名盖章后交付患者
- 处方 PDF 不嵌入任何签章图、名章图或 CA 数字签名
- 处方 PDF 的法律效力定位：仅为内部诊疗记录的电子备份，不具有"可靠电子签名"的法律效力（参见《电子签名法》第13条）；法律效力以纸质处方为准
- 未来若需对接广东省区域卫生信息平台或取得 CA 证书，扩展点（参见 §3.3.2）可平滑接入，届时方可取得"可靠电子签名"法律效力

## 4. 系统整体架构

### 4.1 架构模式
- **客户端**：Windows 桌面原生应用（.NET 10 + WPF MVVM）
- **数据库**：本地 SQLite（敏感字段 AES-GCM 加密）
- **运行环境**：Windows 10 22H2+ / Windows 11 任意版本，离线可运行
- **架构分层**：UI 层 → 应用服务层 → 领域层 → 数据访问层 → 日志层

> **选型说明**：本项目纯 Windows 单机运行，不需要跨平台能力。WPF 在 Windows 打印（尤其是中文针式/票据打印）方面成熟度远高于 Avalonia，且社区资源和文档更丰富。单人开发者学习曲线也更低。

### 4.2 技术选型（v2 修订）

| 关注点 | 选型 | 备注 |
|--------|------|------|
| 运行框架 | .NET 10 LTS | 长期支持至 2028-11（.NET 8 已于 2026-11 到期，不再适用） |
| UI 框架 | WPF | Windows 原生桌面框架；打印支持成熟（FlowDocument / PrintDialog），中文针式/票据打印验证充分 |
| MVVM | CommunityToolkit.Mvvm | 微软官方 MVVM 工具包，Source Generator 自动生成 ViewModel 代码，学习曲线远低于 ReactiveUI |
| ORM | EF Core 10 + Microsoft.Data.Sqlite | 标准 SQLite Provider，无需额外 bundle 配置 |
| 数据库加密 | 字段级 AES-GCM + 标准 SQLite | v1 简化方案：敏感字段加密存储，非敏感字段明文；SQLCipher 全库加密作为 P1 升级目标（见 §4.9） |
| PDF 生成 | QuestPDF 2024.x | Community License（年收入 < $1M 的个人/公司免费使用，非 SaaS 场景合规） |
| Excel 导出 | ClosedXML 0.x | MIT 许可，无商业限制，用于盘点表导出（P1 阶段启用） |
| 扫码枪接入 | USB HID 键盘模式 | 系统级输入事件监听 |
| POS 接入 | 手工录入（P0）/ 小票照片留档 | OCR 识别推迟到 P2 阶段，本期不引入 Tesseract |
| 备份 | 内置 SQLite Backup API + zip 压缩 | 不依赖外部工具 |
| 安全：密码哈希 | PBKDF2-HMAC-SHA256（迭代 ≥ 100,000） | v1 简化方案；Argon2id 作为 P1 升级目标（见 §4.9） |
| 日志校验 | SHA-256 单条哈希 | v1 简化方案：每条日志记录 payload_hash，用于完整性校验；链式哈希作为 P1 升级目标（见 §4.9） |
| 配伍禁忌数据 | DDInter 2.0 离线 CSV | 8 个 CSV 文件导入本地 SQLite（222,383 条 DDI，1,939 种药物），**完全离线运行无需网络**；药品录入需同时填写中英文通用名 |

> **v1 简化原则**：个人开发者第一版优先保证功能正确和合规底线，安全机制采用成熟但较简单的方案。P1 阶段逐步升级到更强的安全方案（SQLCipher 全库加密、Argon2id、链式哈希审计）。

### 4.3 模块划分
1. 用户权限与账号
2. 患者档案 + 电子病历
3. 处方开具 + PDF 打印
4. 收费结算与对账（现金 + 通用 POS）
5. 药品进销存 + 自动盘点 + 效期预警
6. 随访与患者轻服务（合规边界内）
7. 系统日志与合规审计
8. 经营仪表板
9. 备份与恢复子系统
10. 打印模板引擎
11. 数据导入导出工具（FHIR R4、CSV）

### 4.4 数据持久化策略
- **主存**：SQLCipher 加密单文件数据库（clinic.db）
- **WAL 模式** + synchronous=NORMAL，保证单写者场景下的轻量与一致性
- **事务策略**：处方保存、库存扣减、收费确认打包在一个 BEGIN IMMEDIATE ... COMMIT 事务内，避免半成功状态
- **软删除**：所有业务表保留软删标志 deleted_at，物理删除仅在管理员工具内允许
- **唯一索引**：处方编号（年度递增）、药品批号、患者手机号（脱敏后哈希）

### 4.5 离线优先设计原则
- 所有依赖必须为本地 NuGet 离线缓存（开发时 nuget restore 一次，运行时无需公网）
- 不引入需要 ETag / Token / OAuth 的库
- 不引用会悄悄开启 System.Net.Http 调用的 SDK

### 4.6 打印模板引擎
- 模板分类：① 广东省普通门诊处方 ② 急诊处方 ③ A4 病历打印 ④ 5 联收费收据 ⑤ 自定义模板导入
- 模板描述：基于 QuestPDF C# lambda，纯代码定义（不引入 Markdown / HTML 转换以避免不确定性）
- 模板版本号：每次发布对应诊所使用模板变更记录

### 4.7 备份与恢复子系统
- 内置向导：手动备份 / 自动备份配置 / U 盘冷备 / 恢复演练
- 备份文件结构：PRS-BACKUP-{yyyyMMdd-HHmmss}.zip
- 包含：DB dump + 系统日志快照 + 当前打印模板 + 关键配置文件
- 恢复流程：选择 zip → 校验 SHA-256 → 确认覆盖当前库 → 提示覆盖风险 → 完成

### 4.8 预留的逃生口（未来数据迁移）
- 患者、诊断、处方、库存数据可一键导出为 CSV 格式
- 同时提供 SQLite 原始数据库文件导出（供商用 HIS 直接读取或导入）
- 导出操作生成 system_log 行（含导出文件路径、字段范围、操作者）
- 当未来切到商用 HIS 时，可直接使用 CSV 或 SQLite 文件迁移数据（HIS 兼容层不再属于本期目标）
- FHIR R4 导出不在本期范围内——诊所明确不会升级本系统，换 HIS 时 CSV + db 文件迁移更实用

### 4.9 开发分期计划

> **原则**：个人开发者用 AI 工具辅助开发，必须分阶段交付。先跑通核心流程再叠加功能。合规底线（处方保存期限、日志不可删、数据加密）在 P0 就要达标。

#### P0 — MVP 核心（预计 4-6 周）
**目标**：医生能完成"建档→开方→收费→打印→库存扣减"的完整闭环，数据安全合规达标

| 模块 | 内容 | 优先级理由 |
|------|------|-----------|
| 用户权限与账号 | 主诊医生单账号 + 密码登录锁定 | 必须有操作者身份 |
| 患者档案 | 手机号建档 + 基础字段 + 过敏史 | 开方前必须有患者 |
| 处方开具 + PDF | 开方校验（含DDInter配伍禁忌本地查询） + 保存事务 + PDF生成 | 核心业务 |
| 药品进销存 | 入库扫码 + 出库FIFO扣减 + 库存查询 | 处方自动扣库存 |
| 收费（现金） | 确认收款 + payment_log | 闭环必须 |
| 日志审计 | audit_log + system_log（SHA-256单条校验） | 合规底线 |
| 备份恢复 | 自动备份 + 手动恢复 + SHA校验 | 数据安全底线 |
| 打印模板 | 普通门诊处方 + 急诊处方 + 5联收据 | 合规要求纸质处方 |

#### P1 — 完善合规（预计 3-4 周，P0 验收通过后启动）
**目标**：满足卫健飞行检查全部要求，提升日常使用效率

| 模块 | 内容 |
|------|------|
| 安全升级 | SQLCipher全库加密 + Argon2id密码哈希 + U盘密钥双因子 + 链式哈希审计 |
| POS 对账 | 通用商户POS流水号关联 + 日结月结视图 |
| 效期预警 | 分级预警 + 仪表板展示 + 滞销预警 |
| 盘点完善 | 快速抽盘 + 全盘Excel导出(ClosedXML) + 盘盈盘亏报告 |
| 经营仪表板 | 接诊量/处方量/营收/药品消耗统计 |
| 随访服务 | 慢病复诊提醒 + 随访记录登记 |
| A4病历打印 | 病历打印模板 |

#### P2 — 锦上添花（按需迭代，无固定时间）
| 模块 | 内容 |
|------|------|
| OCR识别 | POS小票Tesseract本地识别 |
| 护士账号 | 建档/发药权限分离 |
| 自定义模板 | 诊所自定义打印模板导入 |
| 高级报表 | 按月/季/年统计图表 |
| 数据导出增强 | CSV + SQLite db文件一键导出 |

### 5.1 用户权限与账号体系
- **默认账号**：仅主诊医生（法人）账号
- **账号属性**：username、display_name、role、password_hash(argon2id)、is_active、created_at、last_login_at
- **角色**：doctor（默认）、nurse、readonly
- **激活护士 / 只读账号**：走 *设置 → 账号管理* 受控路径，留痕
- **登录失败锁定**：5 次 / 10 分钟（参数化）
- **会话超时**：无操作 30 分钟自动锁定（参数化）

### 5.2 患者档案 + 电子病历
- **患者基础字段**：姓名、性别、出生日期、手机号（必填，作为患者唯一标识与检索主键）、过敏史、既往史、慢性病标签
- **身份证号**：不采集、不存储。来诊患者不提供身份证号，以手机号作为唯一查询标识
- **脱敏存储规则**：
  - 手机号：SHA-256 哈希值用于检索/唯一索引（不可逆），全号 AES-GCM 加密存储（可逆，用于展示/打印/随访）
  - 姓名不加密（合规需要可直接展示）
- **手机号重复处理**：同一手机号对应一个患者档案，如患者更换手机号需手动更新并记录变更日志
  - 硬必填：主诉、诊断
  - 软必填：查体、辅助检查（未做必须勾选 'N/A'）
- **慢病档案**：血压 / 血糖 / 心率持久化趋势；指标录入弹窗在主诉区可一键调用
- **历史追溯**：患者档案页一键查看历次就诊、处方、收费

### 5.3 处方模块
- **开具流程**：医生点开患者 → 选诊断 → 药品检索（通用名拼音 / 关键字）→ 加入处方表 → 自动校验 → 保存
- **保存触发**：事务内 = ① 处方主表写入 ② 处方明细写入 ③ 病历 / 接诊记录写入 ④ 库存自动扣减 ⑤ PDF 生成 ⑥ sign log 写入
- **校验规则**：
  - 药品品种数超限 → 禁止保存 + 提示
  - 总用药天数超 7 天 → 提示（普通处方提示，急诊拦截）
  - 与上一张处方同通用名 / 同组分（按药品基础数据匹配）→ 重复用药提示
  - 过敏史匹配 → 强制拦截
  - 慢病禁忌：糖尿病患者开含糖颗粒剂 → 提示
  - 配伍禁忌：采用 DDInter 2.0（中南大学湘雅药学院，Nucleic Acids Res 2025;53:D1356）离线数据集，从 https://ddinter2.scbdd.com/download/ 下载 8 个 CSV 文件（按 ATC 分类，共 222,383 条药物-药物交互记录，覆盖 1,939 种药物），导入本地 `drug_interaction` 表。处方校验时用药品英文名做本地查询，**完全离线运行，无需网络**。风险等级：Major → 红色阻断；Moderate → 黄色提示；Minor → 绿色备注；Unknown → 忽略。药品录入时需同时填写中文通用名和英文通用名（用于配伍查询匹配）
- **作废流程**：原 PDF 永久留档 + 标注 *'已作废'* + 写明理由 + 双方签字（如已发药）+ 操作日志
- **打印 / 导出**：PDF 本地导出 + 直接送打印机
- **纸质签名**：处方 PDF 仅作为电子存档，不含签章图；纸质处方由医生手写签名盖章

### 5.4 收费与对账
**注意**：下述收费模式仅适用'非医保定点'。若诊所实际为医保定点（参见 9.2 §R-1），需要走 §3.3.2 插拔模块。

#### 5.4.1 现金
- 处方保存即生成应收金额
- 医生在前台界面点击 *'确认收款'*，写入 payment_log
- 小票自动打印 5 联收据模板，1 联交患者

#### 5.4.2 通用商户 POS（银联 / 微信 / 支付宝）
- **手工录入**：医生前台输入 POS 流水号 + 备注 + 小票照片留档（可选）
- **OCR 自动识别**（P2 阶段）：扫描小票 → 本地识别 → 自动填流水号与金额；v1 阶段不引入 OCR
- **POS SDK 主动回传**（本期不实现，保留扩展点）
- 系统将上述任一通道取得的流水号与处方 ID 强绑定，作为内部对账凭证

#### 5.4.3 对账
- 日结 / 月结视图：现金合计、POS 合计、按处方 ID 可追溯
- 月结报表可导出 CSV
- 长期自动汇总留存

### 5.5 药品进销存 + 自动盘点

#### 5.5.1 入库
- 扫码枪扫码（69 码解析）
- 录入通用名、规格、批号、有效期、供应商、采购价、数量
- **赠品**：单独挂 'cost=0' 标记
- 通用名匹配至 drug_master 自动完成

#### 5.5.2 出库
- 处方保存即 FIFO 自动扣减（近效期批次优先）
- 支持整处方批次混合出库（同一药品可跨批次扣除）
- 出库记录包含批号、扣减数量、单据 ID

#### 5.5.3 盘点
- **快速抽盘**：按最近 30 天消耗 Top N + 近效期药品列表，界面内直接操作比对（P0）
- **全盘**：导出 Excel 盘点表（P1，依赖 ClosedXML），医生实盘数量录入后回传系统，系统比对并产出盘盈 / 盘亏报告
- **盘盈 / 盘亏**：必须填备注 + 报损审批 + 不可删除日志

#### 5.5.4 效期预警
- 分级：6 个月黄、3 个月橙、1 个月红、已过期灰
- 主仪表板突出展示近 7 天内即将过期药品
- 滞销预警：> 90 天未出库

### 5.6 患者增值服务（合规边界）
- 慢病复诊到期提醒（被动列表，不推送）
- 用药禁忌、用法用量查看
- 随访记录登记 + 随访任务提醒（仅诊所界面，不推送患者手机）
- 处方 PDF 本地导出后由医生自行转发患者，**系统不主动发送**
- 红线禁止：线上问诊、远程开方、营销推送、个性化广告

### 5.7 日志审计与导出
- **日志架构**：采用两张核心日志表统一管理：
  - **audit_log**（关键操作日志）：处方操作、病历编辑、库存变动、盘点、报损、收费、退款、POS绑定、导出/打印等关键操作；v1 每条日志记录 payload_hash（SHA-256）用于完整性校验，P1 升级为链式哈希（prev_hash + current_hash + payload_hash）
  - **system_log**（普通操作日志）：登录登出、账号激活、权限变更、配置修改、备份恢复等运维操作，通过 action 字段区分类型（login / account / config / backup / export 等）
- **payment_log** 保持独立（收费结算对账需要独立查询）
- 不可删除、不可清空，磁盘写满时按 60 天滚动压缩
- 卫健飞行检查向导：一键导出 90 天内合规清单（处方、作废、盘点、退药、退费、纸质签名确认缺失）

### 5.8 经营仪表板
- 日 / 周 / 月：接诊量、处方量、营收、现金 / POS 占比
- 药品：消耗 Top 10、毛利率、滞销 Top 10
- 患者：新诊占比、复诊率、过敏史标签覆盖率
- 仅本地展示，无云端同步

## 6. 硬件与部署

### 6.1 硬件清单
| 类别 | 规格 | 数量 |
|------|------|------|
| 主机 | Win10/11，i5 起步，SSD ≥ 256 GB，内存 ≥ 8 GB | 1 |
| 显示器 | 1080p 起步，IPS | 1 |
| USB 扫码枪 | 标准 USB HID 键盘模式 | 1 |
| USB POS 键盘 | 收银用小键盘 | 1 |
| 针式打印机 | 处方 / 收据 5 联 | 1 |
| 激光打印机 | A4 病历打印 | 1 |
| U 盘（冷备） | ≥ 32 GB，专用于每周冷备 | ≥ 1 |
| U 盘（密钥） | ≥ 8 GB，专用于数据库加密密钥因子（P1 阶段启用，与冷备 U 盘物理分离） | 1（P1） |
| UPS | ≥ 600 VA，至少撑 10 分钟 | 1 |

> 注：§7 可移植性提到的密钥 U 盘功能为 P1 阶段安全升级项，v1 阶段仅使用密码派生密钥。

### 6.2 软件清单
- Windows 10 22H2+ 64 位 或 Windows 11 任意版本（.NET 10 不支持已停保的旧版本）
- .NET 10 桌面运行时（内置于安装包）
- 离线地图 / 网络组件：无（本期不需要）

### 6.3 安装与初始化
- 自包含发布包（dotnet publish 自包含模式，无需预装 .NET 运行时）
- 首次启动向导：诊所名称、医生姓名、备份盘选择
- 安装后强制一次备份演练

### 6.4 升级与热更
- 离线系统的升级不基于自动升级，而是'发行 zip 安装包 → 医生本地替换 → 数据库迁移脚本自动执行'
- 数据库迁移脚本按版本号顺序执行，落 migration_log
- 跨大版本升级需备份 + 演练（系统内置向导）

## 7. 非功能需求

| 维度 | 目标 |
|------|------|
| 性能 | 开方到 PDF 生成 ≤ 1 秒；库存扣减事务 ≤ 200 ms；盘点 200 SKU ≤ 3 秒 |
| 并发 | 单医生单账号，无外部并发约束；内部 SQLite 写锁自动排队 |
| 可用性 | 本地系统无外部依赖；月度可用性 ≥ 99.5%（排除人为维护） |
| 可维护性 | UI 与业务分离；任意业务规则修改可由非工程人员配置 + 部署 |
| 数据安全 | 数据库加密 + 密码哈希 + 文件完整性校验；详见 §3.5 |
| 数据 RPO | ≤ 1 天（每日自动备份 + U 盘周冷备） |
| 数据 RTO | ≤ 4 小时（医生可手动恢复） |
| 兼容性 | Win10 22H2+ / Win11 原生 UAC 提示友好；高分屏自适应 |
| 国际化 | 仅简体中文；预留接口但不实施多语言 |
| 法规遵循 | 详见 §3 |
| 可移植性 | 设备唯一标识（P1 阶段绑定密钥 U 盘），v1 阶段仅密码验证；非许可证路径 |

## 8. 测试与验收

### 8.1 单元测试
- 业务规则 / 用药校验 / 配伍禁忌 / 数据加密 / 退药回滚
- 覆盖率目标：核心业务模块 ≥ 60%（P0 MVP 阶段），≥ 80%（P1 阶段）
- 重点测试区域：处方校验逻辑、库存扣减事务、数据加密字段、日志不可删除

### 8.2 集成测试
- 处方 → PDF → 打印 → 库存扣减全链路
- 退药 → 退费 → 日志写入全链路
- 备份 → 恢复演练全链路

### 8.3 卫健飞行检查模拟清单（基于惠城区常见）
1. 随机抽 10 张纸质处方核对 PDF 落档一致性
2. 抽 5 张处方核对医生纸质签名确认
3. 抽查药品进销存台账，与库内实盘对账是否相符
4. 检查近效期预警是否覆盖
5. 检查日志哈希校验是否被篡改

### 8.4 验收用例（P0 必须通过）
- AC-01：建档 / 检索患者档案（手机号为主键）→ 一体化
- AC-02：开方 → 自动校验 → 保存 → PDF生成 + 库存自动扣减
- AC-03：现金收款 → 小票打印 → 日志记录
- AC-04：处方延长 > 7 天 → 弹窗确认 + 理由记录；长期处方 ≤ 28 天（4周）
- AC-05：处方作废 → 留档 + 操作日志
- AC-06：药品入库 → 扫码 → 批号 / 效期录入
- AC-07：备份 zip → 恢复演练 → SHA-256校验通过
- AC-08：日志 SHA-256 校验 → 篡改报警
- AC-09：数据导出 → CSV 文件可正常打开

### 8.5 验收用例（P1 需通过）
- AC-P1-01：POS 流水号关联 → 日结月结对账汇总
- AC-P1-02：效期预警分级展示 + 滞销预警
- AC-P1-03：盘点抽盘 + 全盘 Excel 导出 + 差异报告
- AC-P1-04：SQLCipher 全库加密 + Argon2id密码 + 链式哈希审计
- AC-P1-05：经营仪表板基础统计

## 9. 风险与开放问题

### 9.1 已识别风险

| ID | 风险 | 等级 | 处置 |
|----|------|------|------|
| R-1 | 诊所实际是否医保定点未定 | 高 | 见 §9.2，本文档必须在该决策落定后方可启动开发 |
| R-2 | SQLCipher 商业授权风险（P1升级时） | 中 | v1 使用字段级 AES-GCM + 标准 SQLite，规避授权问题；P1 升级时若冲突，继续使用字段级加密方案 |
| R-3 | 单点硬盘故障风险 | 高 | 强制自动备份 + U 盘冷备 + 季度演练 |
| R-4 | 勒索病毒 / Windows 漏洞 | 中 | 文件落地非系统盘 + 文档化白名单 + 出站策略指引 |
| R-5 | 处方手工签章效力争议 | 低 | 诊所不使用电子签名，处方签章为纸质手写签名（参见 §3.6） |
| R-6 | 季节性峰值下医生负担 | 低 | 全自动出库 + 抽盘 + 红黄橙预警 |
| R-7 | 配伍禁忌数据维护（DDInter 药名为英文通用名） | 中 | 药品录入时必须同时填写中英文通用名；DDInter 数据约每年更新一次，需手动重新下载 CSV 导入；Unknown 等级记录忽略不用于临床提示 |

### 9.2 待用户确认事项

- [R-1] **诊所是否医保定点？** 默认按'非医保定点'推进。若是定点，需要 §3.3.2 插拔范围，对应设计变更 + 工期重新评估。
- [R-2] 诊所是否已备案（备案号、备案机关、备案时间）？—— 用于 5 联收据抬头、医院名称字段。
- [R-3] 处方笺 / 5 联收据是否使用既有定制模板？—— 影响打印模板引擎的初始样例集。
- [R-4] 电脑硬件现状（机型 / 是否含 UPS）？—— 影响 §6.1 风险等级。

## 10. 附录

### 10.1 数据库表结构（草案 / ER 摘要）

`
sys_user(id, username, role, password_hash, is_active, created_at, last_login_at)
patient(id, name, gender, dob, phone_encrypted, phone_hash, allergies, history, chronic_tags, created_at)
medical_record(id, patient_id, doctor_id, visit_at, chief_complaint, present_illness, exam, exam_na,
               auxiliary_exam, auxiliary_na, diagnosis, plan, signature_image_id, created_at, updated_at)
prescription(id, no_year_seq, patient_id, doctor_id, visit_id, diagnosis_code, diagnosis_text,
             total_amount, type_normal|emergency, extended_reason, created_at, pdf_path,
             status active|voided, void_reason, is_paper_signed)
prescription_item(id, prescription_id, drug_id, drug_name, spec, dose, dose_unit,
                  frequency, route, duration_days, qty, batch_id_out, unit_price, subtotal)
drug_master(id, generic_name_cn, generic_name_en, spec, unit, default_usage, is_antibiotic, antibiotic_level,
            is_toxic_drug, contraindication_tags, cost_price_ref, retail_price_ref)
drug_interaction(ddinter_id_a, drug_name_a, ddinter_id_b, drug_name_b, level Major|Moderate|Minor|Unknown,
                 source_atc_code, imported_at)
drug_stock(id, drug_id, batch_no, expiry_date, qty_remaining, cost_price, supplier, received_at)
drug_in(id, drug_id, batch_no, expiry_date, qty, cost_price, supplier, received_at, operator_id)
drug_out(id, drug_id, batch_no, qty, prescription_id, occurred_at, operator_id, is_reversal)
stock_check(id, occurred_at, operator_id, mode spot|full, diff_json, note, signatures_json)
payment_log(id, prescription_id, method cash|pos, amount, occurred_at, operator_id,
            pos_serial_no, pos_image_path, note)
followup(id, patient_id, plan_at, channel onsite|phone, status pending|done|cancelled, note, doctor_id)
audit_log(id, occurred_at, user_id, action, target, payload_hash, prev_hash, hash_chain)
system_log(id, occurred_at, user_id, action, target, note, payload_json)
template(id, name, version, kind prescription|receipt|medical_record, definition_json)
backup_manifest(id, taken_at, source_dir, sha256, file_list_json, size_bytes)
`

> 说明：详细字段后续由 ER 图与迁移脚本给出；此为初版摘要。

### 10.2 第三方依赖清单

| 依赖 | 许可证 | 备注 |
|------|--------|------|
| .NET 10 LTS | MIT | 自包含发布，支持至 2028-11 |
| WPF | MIT | Windows 原生桌面 UI 框架 |
| CommunityToolkit.Mvvm | MIT | 微软官方 MVVM 工具包，Source Generator |
| EF Core 10 | MIT | |
| Microsoft.Data.Sqlite | MIT | 标准 SQLite Provider |
| QuestPDF 2024.x | Community License | 收入 < $1M 的个人/公司免费，非 SaaS 场景合规；商业用途需授权 |
| ClosedXML 0.x | MIT | Excel 导出（盘点表、报表，P1 阶段启用） |
| Serilog | Apache-2.0 | 内部日志，不外发 |

### 10.3 参考文献与法规清单
- 《处方管理办法》（卫生部令第53号）
- 《长期处方管理规范（试行）》（国卫办医发〔2021〕17号）
- 《电子病历应用管理规范（试行）》（国卫办医发〔2017〕8号）
- 《医疗机构病历管理规定（2013年版）》
- 《医院处方点评管理规范（试行）》（卫医管发〔2010〕28号）
- 《抗菌药物临床应用管理办法》
- 《医疗用毒性药品管理办法》（仅作为药品分类参考）
- 《个人信息保护法》
- 《诊所基本标准（2022年版）》及《诊所备案管理暂行办法》
- 广东省 / 惠州市卫生健康委员会最新公开文件（联系窗口核验）
- 国家药品监督管理局《药品说明书范本》
- DDInter 2.0（药物交互数据库，中南大学湘雅药学院，Nucleic Acids Res 2025;53:D1356，离线 CSV 数据来源）
- 《中国药典》2020年版（药品属性参考，在线查阅 https://ydz.chp.org.cn）

### 10.4 文档版本说明
- v1（对话初稿）：包含较显著的合规与技术漏洞
- v2（本文档）：基于对话评审修订 + 合规/技术审查修正 + 个人开发实际约束优化（技术选型简化、分阶段交付、删除OCR/FHIR等非必要模块）
- 后续版本（如有）：仅针对决策变更的小幅修订，不再重写全文

### 10.5 个人开发者方法论

> 本项目由一名开发者使用 AI 辅助工具完成，开发方法论需与团队开发有所不同。

#### 10.5.1 版本控制
- **Git 单分支策略**：main 分支直接开发，每个功能完成即提交；不使用 feature branch 流程（单人无必要）
- **Tag 发布**：每个交付阶段（P0 交付 / P1 升级）打 tag，如 `v0.1.0-p0`、`v1.0.0-p1`
- **提交规范**：简短中文描述，如 `feat: 处方保存事务完成`、`fix: 手机号检索bug修复`

#### 10.5.2 AI 辅助开发工作流
- **代码生成 → 人工审查 → 测试 → 提交**：AI 生成的代码必须逐行审查，尤其是以下高风险区域：
  - 处方校验逻辑（合规红线，AI 可能遗漏边界条件）
  - 数据加密实现（安全敏感，AI 可能生成不安全的方案）
  - 库存扣减事务（并发/一致性敏感）
  - 日志记录（合规要求不可删除，必须确认实现正确）
- **不信任 AI 生成的业务逻辑**：医疗合规规则必须由开发者根据法规原文逐条确认，AI 只辅助编码实现
- **测试先行原则**：先写关键路径的单元测试，再让 AI 辅助生成实现代码

#### 10.5.3 依赖管理原则
- **只用稳定成熟库**：不引入实验性包、预发布版本、或社区维护者少于 3 人的项目
- **最小依赖原则**：能用 .NET 内置 API 完成的功能不引入第三方库（如密码哈希 v1 用内置 PBKDF2 而非 Argon2 库）
- **依赖锁定**：NuGet 包版本锁定到具体版本号，不做浮动版本引用

#### 10.5.4 问题追踪
- 使用项目目录下的 `TODO.md` 文件记录已知问题和待办事项（不引入外部 Issue Tracker）
- 格式：`[P0/P1/P2] 问题描述 → 状态(open/wip/done)`
- 每次提交前检查 TODO.md，确保没有遗漏 P0 级别问题

#### 10.5.5 交付检查清单
- P0 阶段交付前必须完成：
  1. 全部 P0 模块功能可用（手动走完完整业务流程）
  2. 处方保存期限校验正确（手动测试各种天数组合）
  3. 数据加密字段确实加密（检查 db 文件内容）
  4. 日志不可删除（手动尝试删除，验证被阻止）
  5. 备份/恢复演练成功（实际操作一次）
  6. 打印输出格式正确（实际打印一张处方和收据）
  7. 药品库存扣减正确（开方后核对库存数）

