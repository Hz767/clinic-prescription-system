# PRS-PRD-v2.md 审查报告

> 审查对象：个体诊所单机处方管理系统 PRD（v2 修订版）
> 审查日期：2026-07-15
> 审查范围：法规准确性、技术选型时效性、文档内部一致性、业务逻辑合理性、文本错误

---

## 一、法规引用与合规问题（与实际情况不符）

### 1.1 【高】处方保存期限表述不精确

**文档原文**（§3.2.2）：
> 保存期限：门诊病历保存 ≥ 15 年；处方 ≥ 1 年；麻精处方 ≥ 3 年；进销存台账 ≥ 3 年。

**实际情况**（据《处方管理办法》第50条）：
- 普通处方、急诊处方、儿科处方：**1 年**
- 医疗用毒性药品、**第二类精神药品**处方：**2 年**
- 麻醉药品和**第一类精神药品**处方：**3 年**

**问题**：文档将"麻精处方 ≥ 3 年"一刀切，未区分第二类精神药品（2 年）和毒性药品（2 年）。系统如果统一按 3 年设置保存期限，虽不违法但会造成数据冗余；如果按 1 年设置则直接违法。

**建议**：将处方保存期限细化为四档：普通/急诊 1 年 → 毒性药品/精二 2 年 → 麻醉/精一 3 年，并在数据库 prescription 表中按 type 字段关联不同的保存策略。

---

### 1.2 【高】长期处方天数上限与法规不符

**文档原文**（§3.2.1 第4点）：
> 慢病 / 老慢病：必须勾选《延长处方》并填写理由字段，系统默认上限 30 天（按卫健执行口径，可配置）

**实际情况**（据《长期处方管理规范（试行）》国卫办医发〔2021〕17号 第11条）：
> 长期处方的处方量一般**在 4 周内**；根据慢性病特点，病情稳定的患者适当延长，**最长不超过 12 周**。

**问题**：
1. 文档"默认上限 30 天"超出了法规规定的"一般在 4 周（28 天）内"。30 天 > 28 天，需注明理由才合规。
2. 文档未提及最长 12 周（84 天）的法定上限，缺少硬性拦截阈值。
3. 文档未引用《长期处方管理规范（试行）》这一核心法规。

**建议**：修正为"一般 ≤ 28 天；超 28 天需填理由并记录；硬上限 84 天（12 周），超过即拦截"。同时在 §3.1 法规依据中补充《长期处方管理规范（试行）》。

---

### 1.3 【高】麻精药品与长期处方的冲突未说明

**法规明确规定**（《长期处方管理规范》第5条）：
> 医疗用毒性药品、放射性药品、易制毒药品、**麻醉药品、第一类和第二类精神药品**、抗微生物药物……**不得用于长期处方**。

**问题**：文档 §3.2.1 第4点同时设计了"延长处方"和 §3.2.3 麻精药品管理，但未在系统中明确阻断"麻精药品 + 延长处方"的组合。系统若允许对麻精药品开具延长处方，将直接违法。

**建议**：在处方校验规则中增加硬性约束：当药品 `is_narcotic = true` 时，禁止勾选"延长处方"选项，并提示"麻精药品不得用于长期处方"。

---

### 1.4 【高】单人诊所使用麻精药品的资质问题未在待确认事项中提及

**问题**：文档大量设计了麻精药品管理功能（§3.2.3 独立子流程、narcotic_log 表、麻精专方打印等），但单人个体诊所是否具备麻精药品处方权、是否取得印鉴卡，在 §9.2 待确认事项中仅作为 [R-3] 轻描淡写。

**实际情况**：
- 麻醉药品和第一类精神药品处方权需单独审批，且要求医疗机构具备相应资质。
- 麻精药品的采购需要印鉴卡，单人诊所通常不具备。
- §3.2.3 第2点"双人复核：开方 + 复核由医生本人在系统内确认两遍，不强制双人账号"——**这不符合法规要求的"双人"含义**。法规中的"双人复核"是指两个不同的人，不是同一人操作两次。

**建议**：
1. 将 [R-3] 提升为高优先级，明确"若诊所无麻精资质，则整个 §3.2.3 子流程和 narcotic_log 表应设为禁用/隐藏"。
2. 删除或修正"医生本人确认两遍"的描述，改为"单人诊所若需使用麻精药品，必须引入第二人复核并使用独立账号，否则系统应阻止麻精处方开具"。

---

### 1.5 【中】电子签名法律效力的表述存在误导

**文档原文**（§3.6）：
> 处方 PDF 中的电子签章图视为'合法电子签名'效力，按《电子签名法》第 14 条解释由医生本人承担

**实际情况**（据《电子签名法》第13条、第14条）：
- 第14条规定"可靠的电子签名与手写签名或者盖章具有同等的法律效力"。
- 但第13条对"可靠电子签名"有严格条件：专有性（签名制作数据仅由签名人控制）、控制性、改动可知性（签署后对签名改动可被发现）、内容改动可知性。
- **扫描签名图片嵌入 PDF 不满足上述条件**，不具备"可靠电子签名"效力，仅等同于纸质签章的电子化呈现。

**问题**：文档声称"视为合法电子签名效力"存在法律风险，可能误导使用者认为该签名具有完全法律效力。

**建议**：修改为"处方 PDF 嵌入的手写签名扫描图 + 圆形名章图，仅为纸质签章习惯的电子化呈现，不等同于《电子签名法》第13条定义的'可靠电子签名'。其效力参照纸质处方签章管理"。

---

### 1.6 【中】法规引用名称不规范

| 文档中的名称 | 正确名称 | 位置 |
|---|---|---|
| 《电子病历基本规范（试行）》及《电子病历应用管理规范》 | 《电子病历应用管理规范（试行）》（国卫办医发〔2017〕8号），《电子病历基本规范（试行）》已于2017年被替代 | §3.1 |
| 《处方点评管理规范》 | 《医院处方点评管理规范（试行）》（卫医管发〔2010〕28号） | §3.1 |
| 《诊所基本标准 2022》 | 应为《诊所基本标准（2022年版）》或《诊所备案管理暂行办法》（2022年） | §1.4 |

**建议**：统一使用法规全称及文号，避免简称导致的歧义。

---

### 1.7 【中】"中国药典"不是开源数据集

**文档原文**（§5.3）：
> 配伍禁忌：内建字典 + 官方规则集（开源 '中国药典' 数据集）

**问题**：《中国药典》是国家药典委员会编纂的法定标准，**不是开源数据集**。可能存在一些基于药典内容二次整理的开源项目，但直接称"开源中国药典数据集"是不准确的，且存在版权风险。

**建议**：改为"内建配伍禁忌字典（参照《中国药典》2020年版编制），数据源标注来源并保留版本号"。

---

## 二、技术选型问题（与实际情况不符）

### 2.1 【高】.NET 8 LTS 即将到期，应升级到 .NET 10

**文档原文**（§4.2）：
> 运行框架 | .NET 8 LTS | 长期支持至 2026-11

**实际情况**（据 Microsoft 官方支持策略，2026-07 确认）：
- .NET 8 的支持截止日期为 **2026-11-10**，距今仅剩 4 个月，当前已进入 Maintenance 阶段。
- **.NET 10** 已于 2025-11-11 发布，为 LTS 版本，支持至 **2028-11-14**。

**问题**：选择一个即将到期的框架版本作为新系统的技术基座，不合理。系统尚未开发即面临框架到期升级的压力。

**建议**：技术选型升级为 .NET 10 LTS，相应的 EF Core 也升级到 EF Core 10。

---

### 2.2 【中】Avalonia 在纯 Windows 场景下的选型合理性存疑

**文档原文**（§4.2）：
> UI 框架 | Avalonia 11.x | 原生跨平台，但本期只发布 Windows；中文打印 / 表单控件成熟

**问题**：
1. 本项目明确"只发布 Windows"，选择跨平台框架 Avalonia 的核心优势（跨平台）完全用不上。
2. Avalonia 的中文打印生态相比 WPF 较薄弱，文档声称"中文打印 / 表单控件成熟"缺乏依据。处方打印和 5 联收据打印是核心需求，打印稳定性至关重要。
3. WPF 在 Windows 桌面端生态更成熟、社区资源更丰富、打印支持更完善。

**建议**：如果确定只发 Windows，建议改用 WPF（.NET 10 上的 WPF）。如坚持 Avalonia，需在技术评审中明确验证中文针式打印的实际效果。

---

### 2.3 【中】Excel 导出库未列入技术选型

**文档原文**（§5.5.3）：
> 全盘：导出 Excel 盘点表……

**问题**：技术选型表（§4.2）和第三方依赖清单（§10.2）中均未列出 Excel 导出库。.NET 生态中常用的有 EPPlus（LGPL/非商业免费，商业需授权）、NPOI（Apache-2.0）、ClosedXML（MIT）。

**建议**：在技术选型中补充 Excel 导出库（建议 ClosedXML，MIT 许可，无商业限制），并加入第三方依赖清单。

---

### 2.4 【低】QuestPDF 许可证描述不够精确

**文档原文**（§4.2）：
> QuestPDF 2024.x | 社区版可商用（限制为非 SaaS），Apache-2.0 / 双重许可

**问题**：QuestPDF 从 2022.3 起采用 Community License，对年收入低于 100 万美元的公司和个人开发者免费。限制条件不仅是"非 SaaS"，还有收入门槛。"Apache-2.0 / 双重许可"的表述也不准确——QuestPDF 2024.x 是 Community License + Commercial License 双轨，不是 Apache-2.0。

**建议**：核实当前版本的许可条款，精确描述限制条件（收入门槛 + 非SaaS），并在依赖清单中同步修正。

---

## 三、文档内部一致性问题（矛盾 / 不明确）

### 3.1 【高】身份证号存储方式前后矛盾

| 位置 | 描述 |
|---|---|
| §5.2 | "身份证号（哈希后入库 + 可选后四位备查）" |
| §5.2 脱敏存储规则 | "身份证号**全字段** AES-GCM 加密存储" |
| §10.1 patient 表 | `id_card_hash, id_card_last4` |

**问题**：哈希是不可逆的（用于检索/匹配），AES-GCM 加密是可逆的（用于还原原文）。同一字段不可能同时"哈希后入库"和"全字段加密存储"，这是两种完全不同的存储策略。

**建议**：明确为"身份证号 AES-GCM 加密存储（可逆，用于展示/打印），同时存储 SHA-256 哈希值（用于检索/唯一索引），并保留后四位明文备查"。将 patient 表字段修正为 `id_card_encrypted, id_card_hash, id_card_last4`。

---

### 3.2 【高】日志表名在 §5.7 和 §10.1 之间不一致

| §5.7 日志类型 | §10.1 数据库表 | 是否匹配 |
|---|---|---|
| login_log | 无 | ❌ 缺失 |
| prescription_log | 无 | ❌ 缺失 |
| record_log | 无 | ❌ 缺失 |
| stock_log | 无 | ❌ 缺失 |
| payment_log | payment_log | ✅ |
| export_log | 无 | ❌ 缺失 |
| — | audit_log | ❌ §5.7 未提及 |
| — | system_log | ❌ §5.7 未提及 |

**问题**：§5.7 定义的 6 种日志表和 §10.1 的 2 个日志表（audit_log、system_log）完全不对应，开发人员无法确定到底要建哪些表。

**建议**：统一日志表设计。建议保留 audit_log（链式哈希防篡改日志）+ system_log（普通操作日志），将 §5.7 的 login_log、prescription_log、record_log、stock_log、export_log 统一归入 system_log 的 action 字段区分类型。

---

### 3.3 【中】USB 加密狗未在硬件清单中出现

**文档原文**：
- §7 非功能需求 → 可移植性："设备唯一标识（绑定 **USB 加密狗**，便于后续硬件维保），非许可证路径"
- §6.1 硬件清单：未列出加密狗

**问题**：§7 提到系统绑定 USB 加密狗，但 §6.1 硬件清单中没有这个设备。此外，§3.5.2 提到"U 盘加密密钥双因子派生"，U 盘在 §6.1 列出但用途标注为"冷备"——一个 U 盘同时用于冷备和密钥存储，安全风险较高。

**建议**：
1. 如果确实需要加密狗，在 §6.1 硬件清单中补充，并明确与冷备 U 盘是否为同一设备。
2. 建议密钥因子使用独立 U 盘（或加密狗），与冷备 U 盘物理分离，避免丢失冷备盘同时导致无法解密数据库。

---

### 3.4 【中】备份路径硬编码为 D 盘

**文档原文**（§3.5.5）：
> 每日 18:00 自动备份至本地 D 盘 backup/ 目录

**问题**：硬编码"D 盘"假设了用户的分区结构。单盘机器（如部分笔记本）没有 D 盘。同样的问题出现在 §3.5.6"数据库文件落到非系统盘（默认 D 盘）"。

**建议**：改为"自动备份至用户配置的备份目录（默认为系统非系统盘的 backup/ 目录），首次启动向导中让用户选择"。

---

### 3.5 【中】"中成药与西药分方计算"的表述有误

**文档原文**（§3.2.1 第3点）：
> 中成药与西药分方计算的逻辑作为可配置项；当前默认：同方不超过 5 种

**问题**：中药处方和西药处方**分开开具**是法规要求（《处方管理办法》第6条："中药饮片单独开方"），不是"可配置项"。将法定要求降级为"可配置项"会导致合规风险。

**建议**：将中药饮片与西药分方作为**强制规则**，不可配置。中成药是否与西药同方可根据地方卫健委口径确定。

---

### 3.6 【低】版本说明表述不清

**文档原文**（开头）：
> 版本：v2（对应对话评审发现修订）

**问题**："对话评审发现修订"语义不完整，应为"对话评审发现的问题修订"。

---

## 四、业务逻辑问题

### 4.1 【中】旺季日接诊 40+ 的合理性存疑

**文档原文**（§2.3）：
> 旺季日接诊 40+

**问题**：单人诊所（单医生法人执业）日接诊 40+ 人，按每天工作 8 小时计算，平均每位患者仅 12 分钟——含问诊、查体、开方、收费、发药全流程。这个负荷对于单人诊所来说偏高，可能是估算值而非实际数据。

**建议**：核实该数字来源。如为估算，建议标注为"预估峰值"并适当下调（如 30+），或注明"按实际数据校准"。

---

### 4.2 【中】Windows 10 1909+ 版本要求过低

**文档原文**（§4.1、§6.2）：
> Windows 10 1909+ / Windows 11 任意版本

**问题**：Windows 10 1909 已于 2021 年 5 月停止支持（Home/Pro）。.NET 10 的 Windows 支持范围通常从更近的版本开始。以已停止支持的系统版本作为最低要求，存在安全风险和兼容性风险。

**建议**：将最低要求提升为 Windows 10 22H2 或 Windows 11，并核实 .NET 10 的最低 OS 要求。

---

## 五、拼写与格式错误

| 位置 | 错误 | 正确 |
|---|---|---|
| §3.2.3 第1点 | `arcotic_log` | `narcotic_log` |
| §3.5.3 | `udit_log.dat` | `audit_log.dat` |
| §3.5.5 | `ackup/` | `backup/` |
| §4.5 | `uget restore` | `nuget restore` |
| §5.7 | `ecord_log` | `record_log` |

---

## 六、审查总结

| 严重等级 | 数量 | 说明 |
|---|---|---|
| 🔴 高 | 7 | 法规不符（保存期限、长期处方、麻精+长期处方冲突、麻精资质、电子签名误导）+ 内部矛盾（身份证存储、日志表名）+ 技术过期（.NET 8） |
| 🟡 中 | 9 | 法规引用名称、药典数据源、Avalonia选型、Excel库缺失、加密狗遗漏、备份路径、分方规则、接诊量、OS版本 |
| 🟢 低 | 6 | QuestPDF许可、版本说明、5处拼写错误 |

### 优先处理建议

1. **第一优先**：修正所有法规相关问题（1.1–1.7），这些直接影响系统合规性
2. **第二优先**：解决内部矛盾（3.1–3.2），开发团队无法在不明确的设计上开工
3. **第三优先**：升级 .NET 8 → .NET 10（2.1），越晚改成本越高
4. **第四优先**：补全遗漏的技术选型（2.3）和硬件清单（3.3）
5. **第五优先**：修正拼写错误和表述问题
