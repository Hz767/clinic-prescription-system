# 变更日志 (CHANGELOG)

本项目版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范：`主版本.次版本.修订号`

---

## [1.3.0] — 2026-09-16

### 阶段三：个性化配置 + 报表图表化 + 双屏布局 + UI 渲染修复

#### 新增功能（阶段三）
- **医生个性化配置**：医生偏好按登录名本地持久化（`DoctorPreferencesStore`），含快捷词库（可编辑）、常用药品套餐、默认频次/用法/用量预填
- **药品销量 Top 统计**：`BillingService` 多表联查聚合，统计指定日期范围内的药品销量排行
- **经营报表图表化**：经营报表视图新增销量 Top 图表（ProgressBar 模拟条形图 + 数据归一化）
- **双屏布局**：处方预览 / 药品选择可分离到副屏，共享 ViewModel + 多窗口事件订阅实时同步（`DrugPickerSecondaryWindow`、`ToastHostControl`）

#### 安全与健壮性修复
- **库存并发锁**：新增 `InventoryLock.Instance` 共享互斥锁，入库/出库/扣减/退库全程加锁，杜绝并发写冲突
- **事务隔离升级**：`UnitOfWork.BeginTransactionAsync` 使用 `IsolationLevel.Serializable`（SQLite: BEGIN IMMEDIATE），避免写锁升级死锁
- **软删除唯一索引**：5 个唯一索引添加 `[deleted_at] IS NULL` 过滤条件，避免与逻辑删除记录冲突
- **密钥外置**：加密密钥与 Pepper 从 `appsettings.json` 迁至 gitignored `secrets/` 目录，不再硬编码敏感值

#### UI 渲染修复
- **SurfaceBgBrush 缺失崩溃**：`Colors.xaml` 补充 `SurfaceBgBrush` 资源，修复处方页渲染时级联"异常"弹窗堆叠的 P0 故障
- **新患者字段文字不可见**：门诊病历中新患者性别/出生日期/过敏史/基础疾病输入被布局裁剪，调整为紧凑内边距后正常显示
- **处方笺姓名/性别不同步**：新增 `PrescriptionPatientName` / `PrescriptionPatientGender` 显示属性，新患者输入名字后处方笺实时同步（不再回退为「—」）

---

## [1.2.0] — 2026-09-16

### 阶段一：键盘优先交互 + 阶段二：待办进度条与全局反馈

#### 新增功能（阶段一：快捷键+模板）
- **全局快捷键**：主窗口 `Alt+1~7` 快速切换 7 个页面，导航栏当前页高亮
- **处方局部快捷键**：处方页 `F5` 保存、`Ctrl+S` 保存并审核、`Ctrl+N` 新建、`Ctrl+P` 生成 PDF
- **常用处方模板**：可将常用处方存为模板、一键套用或删除，按医生登录名隔离本地存储
- **复制上次处方**：选中患者后一键带出其最近一次处方的诊断与全部药品用法
- **快捷词数字选择**：主诉/诊断联想弹出时按 `1~9` 直接添加、`Esc` 关闭

#### 新增功能（阶段二：待办进度条 + 全局反馈）
- **待办工作台升级为首页**：登录后默认进入工作台，待审核→待收费→待发药一条龙展示
- **处方进度条**：每个待办项显示可视化进度（开方→审核→收费→发药）与下一步操作引导
- **待办"下一步"快捷办理**：点击待办项即跳转收费页对应办理区并预填（审核通过→自动带收费金额；收费完成→自动载入发药区）
- **全局 Toast**：右下角悬浮提示、3 秒自动消失，收费/审核/发药/退费/保存/备份等关键操作反馈即达
- **统一确认框**：作废/退费/审核/恢复等危险操作统一经 `IDialogService` 二次确认

---

## [1.1.0] — 2026-08-13

### 医生姓名编辑 + 输入控件全面修复

#### 新增功能
- **医生姓名编辑**：主窗口新增编辑按钮，支持修改医生显示姓名（用于处方签名），修改后即时更新数据库和当前会话
- **EditNameDialog 对话框**：独立的姓名编辑窗口，含输入验证和错误提示
- **DecimalInputConverter 转换器**：通用十进制输入转换器，支持临时无效输入状态（如单独的小数点），解决 decimal 类型绑定无法输入小数点的问题

#### 修复
- **输入框内容不可见**：修复 TextBox/PasswordBox 自定义模板中 ScrollViewer 布局导致输入内容不可见的问题（Padding 从 ScrollViewer 移至 Border，移除 ScrollViewer 的 VerticalAlignment）
- **体温无法输入小数点**：就诊体征中的体重、体温字段添加 DecimalInputConverter，支持流畅的小数输入
- **处方数量列无法输入小数**：处方明细 DataGrid 的「数量」列（Qty, decimal）添加 DecimalInputConverter
- **患者管理性别选择异常**：性别 ComboBox 从 Text 绑定 + IsEditable 改为 SelectedValue + SelectedValuePath，与自定义模板兼容
- **种子数据医生姓名**：默认管理员姓名从"管理员"改为"陈医生"，符合处方签名规范

#### 优化
- **输入控件视觉反馈**：所有 TextBox/PasswordBox/ComboBox/DatePicker 增强鼠标悬停和键盘聚焦状态（边框变色、边框加粗、背景色变化），提供清晰的选中反馈
- **输入控件全面审查**：系统检查全部 6 个视图中的所有输入控件，确认无其他显示或输入问题

---

## [1.0.0] — 2026-08-13

### 首次正式版本发布

#### 核心功能
- **用户认证**：登录/登出，5 次连续登录失败触发 10 分钟账号锁定，PBKDF2 密码哈希 + Pepper
- **患者管理**：建档/搜索/编辑，支持姓名模糊搜索和手机号精确查找，快速建档含姓名/性别/出生日期/手机号/过敏史/基础疾病
- **处方开具**：完整处方流程（草稿→保存→审核→收费），支持普通/急诊/儿科三种处方类型，≤5 种药品，处方时长限制（3/7/84 天）
- **AI 预审**：7 项规则检查（药物相互作用、过敏史、剂量、重复用药、禁忌、抗生素分级、项目数限制）
- **库存管理**：药品入库/出库/盘点，FIFO 批次扣减，近效期预警
- **收费结算**：现金/POS 收费，已收费处方作废（冲正退款），日结报表
- **PDF 处方**：QuestPDF 生成 A5 处方单，含 Rp 标记，急诊（黄底）/儿科（绿底）区分
- **审计日志**：SHA-256 哈希链（PayloadHash + PrevHash + HashChain），防篡改
- **备份恢复**：WAL checkpoint + Zip 压缩 + SHA-256 校验，恢复需重启应用
- **权限控制**：Doctor/Nurse/Readonly 三角色，Application 层强制权限检查

#### 技术架构
- .NET 10 + WPF + EF Core 10 + SQLite (WAL) + CommunityToolkit.Mvvm
- Clean Architecture Lite（4 层 + 1 共享包）
- 领域层零外部依赖，应用层 FluentValidation 输入验证
- 字段级 AES-GCM 加密（敏感数据）
- LLM 辅助：过敏史结构化整理（Ollama + qwen2.5:7b）

#### 界面特性
- Material Design 风格 UI，自定义图标字典
- 患者联想搜索（300ms 防抖）+ 内联快速建档
- 就诊体征实时验证（两级：黄色警告/红色错误）
- 处方模块可折叠，优化开具流程
- 处方历史双击跳转收费
- DataGrid/ListBox 选中样式修复
- 诊所名称"陈医生诊所"合规校验

#### 开发阶段（14 个阶段）
1. 架构设计与项目搭建
2. DI 容器与 EF Core DbContext 配置
3. P0 核心服务层实现
4. WPF 界面开发
5. P0 验收与后续规划
6. 库存扣减实现（FIFO）
7. FluentValidation 输入验证
8. 药品交互检查 + 作废回退库存
9. 权限控制
10. P0 核心加固（处方校验 + 登录锁定 + 审计日志链 + PDF + 收费 + 备份恢复）
11. P1 功能扩展（库存界面 + 收费流水 + 哈希链验证 + 处方历史）
12. 处方状态机完善 + 已收费处方作废
13. 处方开具界面重构（主诉 + 体征 + 常见诊断 + AI 辅助病历）
14. 集成测试体系建设
